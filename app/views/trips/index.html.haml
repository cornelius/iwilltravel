%p
  = link_to "Plan new trip", :controller => "start"

%p
  = link_to "Plan new trip", :controller => "start", :action => "enter"
  (Alternative startup wizard for comparative UI testing)

%h2 My trips

%ul
- @trips.each do |trip|
  %li
    = link_to trip_title(trip), :controller => "home", :id => trip.id
    %span.destroy-link
      = link_to "Delete", trip_path(trip), :method => :delete
